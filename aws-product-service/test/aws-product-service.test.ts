// import * as cdk from 'aws-cdk-lib';
// import { Template } from 'aws-cdk-lib/assertions';
// import * as AwsProductService from '../lib/aws-product-service-stack';
import * as cdk from 'aws-cdk-lib';
import { Construct } from 'constructs';
import * as product_service from '../lib/product-serivce';

// example test. To run these tests, uncomment this file along with the
// example resource in lib/aws-product-service-stack.ts
test('SQS Queue Created', () => {      
//   const app = new cdk.App();
//     // WHEN
//   const stack = new AwsProductService.AwsProductServiceStack(app, 'MyTestStack');
//     // THEN
//   const template = Template.fromStack(stack);
//   template.hasResourceProperties('AWS::SQS::Queue', {
//     VisibilityTimeout: 300
//   });
});
